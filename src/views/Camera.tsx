import React from 'react';
import { View, Text } from 'react-native';

import CameraService from 'components/CameraService/CameraService';

const Profile = () => <CameraService />;

export default Profile;
